# Inicializamos una variable para almacenar la suma
suma_total = 0

# Iteramos del 1 al 10 (ambos inclusive)
for numero in range(1, 11):
    suma_total += numero

# Imprimimos el resultado
print(f"La suma de los números del 1 al 10 es: {suma_total}")
